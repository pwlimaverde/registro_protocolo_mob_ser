name: upload_remessa_module
description: Modulo para upload das Remessas por arquivo CSV ou XMLS.
publish_to: 'none'
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.1 <3.0.0'
  flutter: ">=3.3.2"

dependencies:
  flutter:
    sdk: flutter
  dependencies_module:
    path: ../dependencies_module

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
